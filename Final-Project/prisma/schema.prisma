generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// --- 1. Users & Auth ---
model User {
  id            String    @id @default(uuid())
  email         String    @unique
  passwordHash  String?   @map("password_hash") // Optional for OAuth users
  isActive      Boolean   @default(true) @map("is_active")
  createdAt     DateTime  @default(now()) @map("created_at")
  lastLoginAt   DateTime? @map("last_login_at")

  // Relations
  profile          Profile?
  userRoles        UserRole[]
  ownedRooms       Room[]            @relation("RoomOwner")
  hostedCalls      Call[]            @relation("CallHost")
  callParticipation CallParticipant[]
  sentMessages     Message[]
  devices          Device[]
  sentInvitations  Invitation[]      @relation("Inviter")
  receivedInvites  Invitation[]      @relation("Invitee")
  auditLogs        AuditLog[]

  @@map("users")
}

model Profile {
  userId      String   @id @map("user_id") // One-to-One: PK is also FK
  displayName String?  @map("display_name")
  avatarUrl   String?  @map("avatar_url")
  bio         String?
  locale      String?  @db.VarChar(10)
  timezone    String?  @db.VarChar(50)
  phone       String?  @db.VarChar(30)
  updatedAt   DateTime @updatedAt @map("updated_at")

  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("profiles")
}

model Role {
  id   Int        @id @default(autoincrement()) @db.SmallInt
  name String     @unique @db.VarChar(50)

  userRoles UserRole[]

  @@map("roles")
}

model UserRole {
  userId     String    @map("user_id")
  roleId     Int       @map("role_id") @db.SmallInt
  scopeType  String?   @map("scope_type") @db.VarChar(50) // e.g. "SYSTEM", "ROOM"
  scopeId    String?   @map("scope_id")
  assignedAt DateTime  @default(now()) @map("assigned_at")

  user       User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  role       Role      @relation(fields: [roleId], references: [id], onDelete: Cascade)

  @@id([userId, roleId]) // Composite Key
  @@map("user_roles")
}

// --- 2. Rooms & Calls ---
model Room {
  id              String   @id @default(uuid())
  ownerId         String   @map("owner_id")
  name            String   @db.VarChar(200)
  slug            String   @unique @db.VarChar(100)
  isPrivate       Boolean  @default(false) @map("is_private")
  maxParticipants Int?     @map("max_participants")
  createdAt       DateTime @default(now()) @map("created_at")
  settings        Json?    // Using JSON for flexible settings

  owner           User     @relation("RoomOwner", fields: [ownerId], references: [id], onDelete: Cascade)
  calls           Call[]
  invitations     Invitation[]
  messages        Message[] // Room-level chat history
  auditLogs       AuditLog[]

  @@map("rooms")
}

model Call {
  id          String    @id @default(uuid())
  roomId      String    @map("room_id")
  hostUserId  String    @map("host_user_id")
  startedAt   DateTime  @default(now()) @map("started_at")
  endedAt     DateTime? @map("ended_at")
  status      String    @db.VarChar(20) // "ONGOING", "ENDED"
  metadata    Json?

  room         Room              @relation(fields: [roomId], references: [id], onDelete: Cascade)
  host         User              @relation("CallHost", fields: [hostUserId], references: [id])
  participants CallParticipant[]
  messages     Message[]         // Call-specific chat
  auditLogs    AuditLog[]

  @@map("calls")
}

model CallParticipant {
  id             String    @id @default(uuid())
  callId         String    @map("call_id")
  userId         String    @map("user_id")
  displayName    String?   @map("display_name")
  joinedAt       DateTime  @default(now()) @map("joined_at")
  leftAt         DateTime? @map("left_at")
  isMuted        Boolean   @default(false) @map("is_muted")
  isVideoEnabled Boolean   @default(true) @map("is_video_enabled")
  roleInCall     String?   @map("role_in_call") @db.VarChar(30) // "HOST", "GUEST"
  connectionInfo String?   @map("connection_info")

  call   Call @relation(fields: [callId], references: [id], onDelete: Cascade)
  user   User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("call_participants")
}

// --- 3. Communications ---
model Message {
  id           String   @id @default(uuid())
  roomId       String?  @map("room_id")
  callId       String?  @map("call_id")
  senderUserId String   @map("sender_user_id")
  content      String
  contentType  String   @default("text") @map("content_type") @db.VarChar(20)
  createdAt    DateTime @default(now()) @map("created_at")
  metadata     Json?

  room   Room? @relation(fields: [roomId], references: [id])
  call   Call? @relation(fields: [callId], references: [id])
  sender User  @relation(fields: [senderUserId], references: [id])

  @@map("messages")
}

model Invitation {
  id            String   @id @default(uuid())
  roomId        String   @map("room_id")
  inviterUserId String   @map("inviter_user_id")
  inviteeUserId String   @map("invitee_user_id")
  token         String   @unique @db.VarChar(128)
  expiresAt     DateTime @map("expires_at")
  status        String   @db.VarChar(20) // "PENDING", "ACCEPTED", "DECLINED"
  createdAt     DateTime @default(now()) @map("created_at")
  metadata      Json?

  room    Room @relation(fields: [roomId], references: [id], onDelete: Cascade)
  inviter User @relation("Inviter", fields: [inviterUserId], references: [id])
  invitee User @relation("Invitee", fields: [inviteeUserId], references: [id])

  @@map("invitations")
}

// --- 4. System & Logs ---
model Device {
  id                 String   @id @default(uuid())
  userId             String   @map("user_id")
  deviceType         String   @map("device_type") @db.VarChar(50)
  clientId           String   @map("client_id") @db.VarChar(255)
  lastSeen           DateTime @map("last_seen")
  pushToken          String?  @map("push_token")
  connectionMetadata Json?    @map("connection_metadata")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("devices")
}

model AuditLog {
  id        String   @id @default(uuid())
  userId    String   @map("user_id")
  roomId    String?  @map("room_id")
  callId    String?  @map("call_id")
  action    String   @db.VarChar(100) // "USER_LOGIN", "ROOM_CREATED"
  details   Json?
  createdAt DateTime @default(now()) @map("created_at")

  user User  @relation(fields: [userId], references: [id])
  room Room? @relation(fields: [roomId], references: [id])
  call Call? @relation(fields: [callId], references: [id])

  @@map("audit_logs")
}